import React, { useEffect, useState, useRef } from "react";

import "./Settings.css";

function Settings(props) {
  return (
    <div>
      <button className="button" style={{top: window.innerHeight-60, left: window.innerWidth-60}}>&#9998;</button>
    </div>
  );
}

export default Settings;
